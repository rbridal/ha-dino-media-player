{
  "config": {
    "step": {
      "user": {
        "title": "Dino Yard Player",
        "description": "Match the MQTT topic prefix in /opt/dino-media-player/config.yaml on the Pi.",
        "data": {
          "name": "Device name",
          "topic_prefix": "MQTT topic prefix"
        }
      }
    },
    "abort": {
      "already_configured": "A player with this topic prefix is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Dino Yard Player",
        "description": "Offline alerts fire after the player stays unavailable for the chosen number of minutes. Pick a notify service or notify group (Rob's iPhone).",
        "data": {
          "name": "Device name",
          "topic_prefix": "MQTT topic prefix",
          "alert_enabled": "Notify when offline",
          "alert_minutes": "Minutes offline before notify",
          "notify_service": "Notifier"
        }
      }
    }
  }
}
