{
  "config": {
    "step": {
      "user": {
        "title": "Dino Yard Player",
        "description": "Match the MQTT topic prefix in /opt/dino-media-player/config.yaml on the Pi.",
        "data": {
          "name": "Device name",
          "topic_prefix": "MQTT topic prefix"
        }
      }
    },
    "abort": {
      "already_configured": "A player with this topic prefix is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Dino Yard Player",
        "description": "Uses one notifier and one delay for two conditions: the player stays unavailable, or Bluetooth is selected as the output and stays disconnected.",
        "data": {
          "name": "Device name",
          "topic_prefix": "MQTT topic prefix",
          "alert_enabled": "Notify when offline or Bluetooth is down",
          "alert_minutes": "Minutes before notify",
          "notify_service": "Notifier"
        }
      }
    }
  }
}
